# SvelteKit Notes

A simple local note-taking app built with SvelteKit and TailwindCSS.

## Features

- Create, edit, pin, delete, and search notes
- Add a lightweight tag to each note
- Persist notes in browser storage
- Responsive two-pane layout

## Run

```sh
npm install
npm run dev
```

Then open the local URL printed by Vite.
